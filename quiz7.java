public class quiz7 {
    // reverse a number

    // check if a number 'p' is a factor of another number 'N'

    // check if a number is a prime number or not
    /*
    math.sqrt
    while (i*i <= n)  while (i <= math.sqrt(n)
    n % i == 0
     */

    // check if a number is perfect square or not
}
